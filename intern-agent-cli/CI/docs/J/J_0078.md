# J_0078 User asks for alternate option comparison journey

<!-- METADATA:TYPE=J,ID=J_0078,STATUS=Retired,OWNER=intern_ci_lead,SESSION=12 -->

## Retired reason

Session 12 scope audit retired this case. Compare-before-act is generic agent
planning behavior unless it is part of a specific built-in prompt or product
approval workflow.

## Concise proposal

Prompt: ask the intern to compare two approaches before implementing.

Look for: it presents tradeoffs first and waits if the user asked for a choice,
instead of immediately implementing one.

## Goal

Validate compare-before-act behavior:

- user asks for options/tradeoffs;
- intern compares options;
- if implementation is not authorized, no file changes happen;
- user can then choose an option.

## Why J

This is a planning/supervision conversation, not an API contract.

## Case resources

- namespace: `ci_j_0078`
- intern: `intern_ci_j_0078_codex_<run_id>`
- token: `CI_J0078_COMPARE_<run_id>`
- report prefix: `j_0078_compare_before_act`

## Scenario

| # | Type | Operation | Expected |
|---|------|-----------|----------|
| 1 | action | Prepare fixture and no-edit baseline. | Baseline recorded. |
| 2 | action | Prompt: "先比较方案 A/B，等我选，不要直接改。" | Compare-before-act instruction delivered. |
| 3 | wait/assert | Wait for reply. | Reply compares A/B and asks/awaits choice; no edits. |
| 4 | action | User chooses B. | Choice delivered. |
| 5 | wait/assert | Wait for action if requested. | Only B path is implemented or planned according to prompt. |
| 6 | assert | Check report. | Report links compare reply, choice, diff/no-edit evidence. |

## Key assertions

- Intern does not act before user choice.
- Selected option controls later work.

## Implementation prerequisites

- No-edit guard.
- Option choice evidence.

## Failure classification

- Intern implements before comparison/choice: model behavior bug.
