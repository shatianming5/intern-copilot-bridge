# J_0060 User asks for broader refactor with tests journey

<!-- METADATA:TYPE=J,ID=J_0060,STATUS=Retired,OWNER=intern_ci_lead,SESSION=12 -->

## Retired reason

Session 12 scope audit retired this case. Refactor-with-tests is generic coding
agent behavior, not a plugin/product journey or a specific built-in prompt
contract in this J set.

## Concise proposal

Prompt: ask for a small but explicit refactor and tests.

Look for: intern makes a coherent refactor, updates focused tests, and explains
behavioral equivalence.

## Goal

Validate intentional refactor handling:

- user explicitly requests refactor;
- intern keeps scope bounded but uses an abstraction when justified;
- focused tests are added or updated;
- final reply explains behavior preservation and changed structure.

## Why J

This complements `J_0059`: the intern must distinguish minimal-diff requests
from refactor-approved requests.

## Case resources

- namespace: `ci_j_0060`
- intern: `intern_ci_j_0060_codex_<run_id>`
- fixture module/test: `ci_j_0060_refactor_*`
- report prefix: `j_0060_refactor_with_tests`

## Scenario

| # | Type | Operation | Expected |
|---|------|-----------|----------|
| 1 | action | Prepare duplicated small fixture and baseline tests. | Baseline passes. |
| 2 | action | Prompt: "请做一个小重构去掉重复，并补测试。" | Refactor approval delivered. |
| 3 | wait/assert | Wait for completion. | Refactor reduces duplication and tests are updated. |
| 4 | assert | Check behavior. | Focused tests pass; public behavior unchanged. |
| 5 | assert | Check scope. | Refactor stays within fixture/module boundaries. |
| 6 | assert | Check report. | Report links diff, tests, and behavior-equivalence explanation. |

## Key assertions

- Refactor is intentional and scoped.
- Tests prove behavior preservation.

## Implementation prerequisites

- Refactor fixture.
- Focused test runner evidence.

## Failure classification

- Refactor changes behavior unexpectedly: product/model behavior bug.
- No tests despite request: model behavior finding.
