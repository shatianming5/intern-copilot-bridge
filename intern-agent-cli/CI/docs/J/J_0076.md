# J_0076 Output format strict table journey

<!-- METADATA:TYPE=J,ID=J_0076,STATUS=Retired,OWNER=intern_ci_lead,SESSION=12 -->

## Retired reason

Session 12 scope audit retired this case. Strict table-only output is generic
format-following behavior and should not be a standalone J journey.

## Concise proposal

Prompt: ask for a strict markdown table with three rows and no prose.

Look for: reply follows the requested shape exactly enough for parser use, and
does not add narrative outside the table.

## Goal

Validate strict output-format following:

- user requests table-only output;
- intern replies with only the table;
- row count and columns match;
- no extra prose appears.

## Why J

This is a common user-agent interaction requirement for downstream copying.

## Case resources

- namespace: `ci_j_0076`
- intern: `intern_ci_j_0076_codex_<run_id>`
- token: `CI_J0076_TABLE_<run_id>`
- report prefix: `j_0076_strict_table_output`

## Scenario

| # | Type | Operation | Expected |
|---|------|-----------|----------|
| 1 | action | Create intern/session/group. | Group green. |
| 2 | action | Prompt strict 3-row markdown table with token in one cell and no prose. | Format instruction delivered. |
| 3 | wait/assert | Wait for reply. | Reply is table-only with expected rows/columns/token. |
| 4 | assert | Check report. | Report links prompt/reply and parser result. |

## Key assertions

- Strict format is followed.
- No extra narrative outside table.

## Implementation prerequisites

- Markdown table parser/classifier.

## Failure classification

- Extra prose breaks table-only contract: model behavior finding.
