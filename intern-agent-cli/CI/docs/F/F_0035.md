# F_0035 Config card cancel no-mutation contract

<!-- METADATA:TYPE=F,ID=F_0035,STATUS=ApprovedForImplementation,OWNER=intern_ci_lead,SESSION=62 -->

## 目标

覆盖 `/config` card 的 Cancel 交互。主管已明确所有 config card 都需要 Cancel，防止误触后只能保存。

## 资源

- namespace: `ci_f_0035`。
- project: `ci_f_0035_project_{run_id}`。
- intern: `intern_ci_f_0035_codex_{run_id}`。

## 剧本

| # | 类型 | 操作 | 注释 |
|---|------|------|------|
| 1 | action | `action_reset_case_namespace(case_id="F_0035")` | 清理本 case group/config/card callback state。 |
| 2 | action | `action_seed_group(project=project, intern=intern)` | 构造已绑定 intern 群。 |
| 3 | action | `action_send_slash(chat_id=chat_id, text="/config")` | 打开 `/config`。 |
| 4 | wait | `wait_for_config_card(chat_id=chat_id)` | 等待 card payload。 |
| 5 | assert | `assert_config_card_actions(actions=["save", "cancel"])` | 断言 Save 和 Cancel 都存在。当前只有 Save 应报产品 bug。 |
| 6 | action | `action_capture_group_config(project=project, intern=intern)` | 记录 Cancel 前 trigger/detail/no-collapse。 |
| 7 | action | `action_click_config_cancel(card=card)` | 模拟 owner 点击 Cancel。 |
| 8 | assert | `assert_config_unchanged_after_cancel(project=project, intern=intern)` | Cancel 不写 relay/daemon config，不触发 description sync。 |
| 9 | assert | `assert_config_card_cancel_state(card=card)` | 断言 card 更新为取消/关闭状态，或至少不会继续误提交旧表单。 |
| 10 | action | `action_submit_config_save(trigger_mode="at_only", detail_mode="summary", no_collapse_mode="off")` | 覆盖 Save 仍可用。 |
| 11 | assert | `assert_config_saved(project=project, intern=intern, trigger_mode="at_only", detail_mode="summary", no_collapse_mode="off")` | Save 写入配置，与 Cancel no-mutation 区分。 |

## 通过标准

- `/config` card 有 Cancel。
- Cancel 无配置副作用。
- Save 路径仍能保存三类 mode。

## 失败分类

- `product_bug_config_card_missing_cancel`
- `product_bug_config_cancel_mutates_state`
- `product_bug_config_cancel_leaves_submit_live`

## 分发建议

分配给 `intern_ci_worker3`，与 `F_0016/F_0017` correction 一起处理。

